version: '3.8'
services:
  pdfmedic:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pdfmedic
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=3000
